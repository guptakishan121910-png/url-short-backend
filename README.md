# url-short-backend

Express and MongoDB backend for the Trim URL shortener.

## Run locally

```bash
npm install
cp .env.example .env
npm run dev
```

The API runs on `http://localhost:4000` by default.
